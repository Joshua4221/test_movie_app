// export const playVideo = async (index: number, videoRefs) => {
//   const video = videoRefs.current[index];
//   if (video) {
//     await video.playAsync();
//     setIsPlaying(true);
//   }
// };

// export const pauseVideo = async (index: number, videoRefs) => {
//   const video = videoRefs.current[index];
//   if (video) {
//     await video.pauseAsync();
//     setIsPlaying(false);
//   }
// };

// export const startVideo = async (index: number, videoRefs) => {
//   const video = videoRefs.current[index];
//   if (video) {
//     await video.replayAsync();
//     setIsPlaying(false);
//   }
// };
